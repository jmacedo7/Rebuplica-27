import type { IncomingMessage, ServerResponse } from 'node:http';
import { ApiError } from './errors.ts';
export const json=(response:ServerResponse,status:number,body:unknown):void=>{response.statusCode=status;response.setHeader('content-type','application/json; charset=utf-8');response.end(JSON.stringify(body));};
export const parseJson=async(request:IncomingMessage):Promise<Record<string,unknown>>=>{const chunks:Buffer[]=[];let size=0;for await(const chunk of request){const buf=Buffer.isBuffer(chunk)?chunk:Buffer.from(chunk);size+=buf.length;if(size>1024*1024)throw new ApiError(413,'PAYLOAD_TOO_LARGE','Request body is too large');chunks.push(buf);}if(!chunks.length)return{};let value:unknown;try{value=JSON.parse(Buffer.concat(chunks).toString('utf8'));}catch{throw new ApiError(400,'INVALID_JSON','Request body must be valid JSON');}if(!value||typeof value!=='object'||Array.isArray(value))throw new ApiError(400,'INVALID_BODY','Request body must be an object');return value as Record<string,unknown>;};
export const requireString=(body:Record<string,unknown>,key:string,max=500):string=>{const value=body[key];if(typeof value!=='string'||value.trim()===''||value.length>max)throw new ApiError(400,'VALIDATION_ERROR',`${key} is invalid`);return value.trim();};
