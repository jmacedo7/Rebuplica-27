import { createHmac,randomBytes,timingSafeEqual } from 'node:crypto';
export interface AccessClaims{readonly sub:string;readonly iss:string;readonly aud:string;readonly iat:number;readonly exp:number;readonly jti:string;}
const b64=(value:string|Buffer):string=>Buffer.from(value).toString('base64url');const sign=(input:string,secret:string):string=>b64(createHmac('sha256',secret).update(input).digest());
export const issueAccessToken=(subject:string,secret:string,issuer:string,audience:string,ttlSeconds=900,nowSeconds=Math.floor(Date.now()/1000)):string=>{const header=b64(JSON.stringify({alg:'HS256',typ:'JWT'}));const payload=b64(JSON.stringify({sub:subject,iss:issuer,aud:audience,iat:nowSeconds,exp:nowSeconds+ttlSeconds,jti:randomBytes(16).toString('hex')}));return`${header}.${payload}.${sign(`${header}.${payload}`,secret)}`;};
export const verifyAccessToken=(token:string,secret:string,issuer:string,audience:string,nowSeconds=Math.floor(Date.now()/1000)):AccessClaims=>{const parts=token.split('.');if(parts.length!==3)throw new Error('Invalid access token');const[header,payload,signature]=parts;if(!header||!payload||!signature)throw new Error('Invalid access token');const expected=Buffer.from(sign(`${header}.${payload}`,secret));const actual=Buffer.from(signature);if(expected.length!==actual.length||!timingSafeEqual(expected,actual))throw new Error('Invalid access token');let body:unknown;try{body=JSON.parse(Buffer.from(payload,'base64url').toString('utf8'));}catch{throw new Error('Invalid access token');}if(!body||typeof body!=='object')throw new Error('Invalid access token');const claims=body as Record<string,unknown>;if(typeof claims.sub!=='string'||claims.iss!==issuer||claims.aud!==audience||typeof claims.iat!=='number'||typeof claims.exp!=='number'||typeof claims.jti!=='string'||claims.exp<=nowSeconds)throw new Error('Invalid access token');return claims as unknown as AccessClaims;};
