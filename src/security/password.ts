import { randomBytes,scryptSync,timingSafeEqual } from 'node:crypto'; const KEY_LENGTH=64;const PREFIX='scrypt';
export const hashPassword=(password:string):string=>{if(password.length<12)throw new Error('Password must contain at least 12 characters');const salt=randomBytes(16);const derived=scryptSync(password,salt,KEY_LENGTH,{N:32768,r:8,p:1,maxmem:64*1024*1024});return`${PREFIX}$${salt.toString('base64url')}$${derived.toString('base64url')}`;};
export const verifyPassword=(password:string,encoded:string):boolean=>{const[prefix,saltText,hashText]=encoded.split('$');if(prefix!==PREFIX||!saltText||!hashText)return false;try{const salt=Buffer.from(saltText,'base64url');const expected=Buffer.from(hashText,'base64url');const actual=scryptSync(password,salt,expected.length,{N:32768,r:8,p:1,maxmem:64*1024*1024});return expected.length===actual.length&&timingSafeEqual(expected,actual);}catch{return false;}};
